<UserControl x:Class="TT.Report.Views.ServiceStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:TT.Report.Views"             
             xmlns:conv="clr-namespace:TT.Converters;assembly=TT.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="172" d:DesignWidth="315">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Icons.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/FlatSlider.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Themes/SplitButton.xaml" />
                <ResourceDictionary>
                    <SolidColorBrush x:Key="GrayBrush2">#FF7F7F7F</SolidColorBrush>
                    <SolidColorBrush x:Key="GrayBrush5">#FFB9B9B9</SolidColorBrush>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid ShowGridLines="False" Height="172" Width="315" x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MinHeight="25"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*" MinWidth="57"/>
            <ColumnDefinition Width="*" MinWidth="57"/>
            <ColumnDefinition Width="*" MinWidth="57"/>
            <ColumnDefinition Width="*" MinWidth="57"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="4" Background="{StaticResource GrayBrush5}"/>
        <Border Grid.Row="3" Grid.Column="5" Background="{StaticResource GrayBrush2}"/>
        <Border Grid.Row="0" Grid.Column="5" Grid.RowSpan="4" BorderBrush="Black" BorderThickness="2 0 0 0" />
        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="6" BorderBrush="Black" BorderThickness="0 2 0 0"/>
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="6" BorderBrush="Black" BorderThickness="0 0 0 1" />
        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="6" BorderBrush="Black" BorderThickness="0 0 0 1"/>
        <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="4" BorderBrush="Black" BorderThickness="0 0 1 0" />
        <Border Grid.Row="0" Grid.Column="2" Grid.RowSpan="4" BorderBrush="Black" BorderThickness="1 0 0 0"/>
        <Border Grid.Row="0" Grid.Column="3" Grid.RowSpan="4" BorderBrush="Black" BorderThickness="1 0 0 0"/>
        <Border Grid.Row="0" Grid.Column="4" Grid.RowSpan="4" BorderBrush="Black" BorderThickness="1 0 0 0"/>


        <TextBlock Grid.Row="0" Grid.Column="1"  HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="10" Text="Pendulum"/>
        <TextBlock Grid.Row="0" Grid.Column="2"  HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="10" Text="Reverse"/>
        <TextBlock Grid.Row="0" Grid.Column="3"  HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="10" Text="Tomahawk"/>
        <TextBlock Grid.Row="0" Grid.Column="4"  HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="10" Text="Special"/>
        <TextBlock Grid.Row="0" Grid.Column="5"  HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"  Text="∑"/>
        <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"  Text="FH"/>
        <TextBlock Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Text="BH"/>
        <TextBlock Grid.Row="3" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Text="∑"/>

        <Grid Grid.Row="1" Grid.Column="1" Margin="0 0 0 1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="ForehandPendulumTotalButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandPendulumTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandPendulumPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandPendulumPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandPendulumDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandPendulumDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandPendulumPointsLostButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandPendulumPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0 0 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="BackhandPendulumTotalButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandPendulumTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandPendulumPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandPendulumPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandPendulumDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandPendulumDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandPendulumPointsLostButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandPendulumPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="3" Grid.Column="1" Margin="0 2 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="AllPendulumTotalButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand'|| r.Strokes[0].Side == null) &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllPendulumTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllPendulumPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllPendulumPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllPendulumDirectPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllPendulumDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllPendulumPointsLostButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Pendulum' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllPendulumPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="2" Margin="1 0 0 1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="ForehandReversePendulumTotalButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandReversePendulumTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandReversePendulumPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandReversePendulumPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandReversePendulumDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandReversePendulumDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandReversePendulumPointsLostButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandReversePendulumPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="2" Margin="1 0 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="BackhandReversePendulumTotalButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandReversePendulumTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandReversePendulumPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandReversePendulumPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandReversePendulumDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandReversePendulumDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandReversePendulumPointsLostButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandReversePendulumPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="3" Grid.Column="2" Margin=" 1 2 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="AllReversePendulumTotalButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllReversePendulumTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllReversePendulumPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllReversePendulumPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllReversePendulumDirectPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllReversePendulumDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllReversePendulumPointsLostButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Reverse' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllReversePendulumPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="3" Margin="1 0 0 1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="ForehandTomahawkTotalButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandTomahawkTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandTomahawkPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandTomahawkPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandTomahawkDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandTomahawkDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandTomahawkPointsLostButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandTomahawkPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="3" Margin="1 0 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="BackhandTomahawkTotalButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandTomahawkTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandTomahawkPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandTomahawkPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandTomahawkDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandTomahawkDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandTomahawkPointsLostButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandTomahawkPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="3" Grid.Column="3" Margin="1 2 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="AllTomahawkTotalButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllTomahawkTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllTomahawkPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllTomahawkPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllTomahawkDirectPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllTomahawkDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllTomahawkPointsLostButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Tomahawk' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllTomahawkPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="4" Margin="1 0 0 1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="ForehandSpecialTotalButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandSpecialTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandSpecialPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandSpecialPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandSpecialDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandSpecialDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandSpecialPointsLostButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandSpecialPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="4" Margin="1 0 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="BackhandSpecialTotalButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandSpecialTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandSpecialPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandSpecialPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandSpecialDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandSpecialDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandSpecialPointsLostButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandSpecialPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="3" Grid.Column="4" Margin="1 2 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="AllSpecialTotalButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Special'" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllSpecialTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllSpecialPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllSpecialPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllSpecialDirectPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllSpecialDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllSpecialPointsLostButton" Tag="r => (r.Strokes[0].Side == 'Forehand' || r.Strokes[0].Side == 'Backhand') &amp;&amp; r.Strokes[0].Servicetechnique == 'Special' &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllSpecialPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="5" Margin="2 0 0 1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="ForehandAllTotalButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special')" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandAllTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandAllPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandAllPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandAllDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandAllDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="ForehandAllPointsLostButton" Tag="r => r.Strokes[0].Side == 'Forehand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=ForehandAllPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="5" Margin="2 0 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="BackhandAllTotalButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special')" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandAllTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandAllPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandAllPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandAllDirectPointsWonButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandAllDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="BackhandAllPointsLostButton" Tag="r => r.Strokes[0].Side == 'Backhand' &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=BackhandAllPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="3" Grid.Column="5" Margin="2 2 0 0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="AllServicesTotalButton" Tag="r => (r.Strokes[0].Side == 'Backhand' || r.Strokes[0].Side == 'Forehand') &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special')" Style="{DynamicResource MetroAccentFlatGrayCheckedToggle}" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="1"  Padding="2 0 2 0"  FontSize="32"   >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllServicesTotalButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllServicesPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Backhand' || r.Strokes[0].Side == 'Forehand') &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player == r.Winner" Style="{DynamicResource MetroAccentFlatGrayCheckedToggle}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Padding="2 0 2 0"  FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllServicesPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllServicesDirectPointsWonButton" Tag="r => (r.Strokes[0].Side == 'Backhand' || r.Strokes[0].Side == 'Forehand') &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player == r.Winner &amp;&amp; r.Length &lt; 3" Style="{DynamicResource MetroAccentFlatGrayCheckedToggle}" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch"  Padding="1 0 1 0"     Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllServicesDirectPointsWonButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
            <ToggleButton x:Name="AllServicesPointsLostButton" Tag="r => (r.Strokes[0].Side == 'Backhand' || r.Strokes[0].Side == 'Forehand') &amp;&amp; (r.Strokes[0].Servicetechnique == 'Pendulum' || r.Strokes[0].Servicetechnique == 'Reverse' || r.Strokes[0].Servicetechnique == 'Tomahawk' || r.Strokes[0].Servicetechnique == 'Special') &amp;&amp; r.Strokes[0].Player != r.Winner" Style="{DynamicResource MetroAccentFlatGrayCheckedToggle}" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Stretch"  HorizontalAlignment="stretch" Padding="1 0 1 0" FontWeight="Bold"    Visibility="Visible">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="StatButtonClick">
                            <cal:Parameter Value="{Binding ElementName=StatisticsContainer}" />
                            <cal:Parameter Value="{Binding ElementName=AllServicesPointsLostButton, Path=Name}" />
                        </cal:ActionMessage>
                        <cal:ActionMessage MethodName="SelectService">
                            <cal:Parameter Value="$source" />
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ToggleButton.Content>
                    <MultiBinding Converter="{conv:RallyExpressionToCountConverter}" Mode="OneWay">
                        <Binding Path="BasicFilterStatisticsView.SelectedRallies" />
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                    </MultiBinding>
                </ToggleButton.Content>
            </ToggleButton>
        </Grid>
    </Grid>

</UserControl>
