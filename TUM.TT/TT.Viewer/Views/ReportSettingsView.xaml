<Controls:MetroWindow x:Class="TT.Viewer.Views.ReportSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prop="clr-namespace:TT.Viewer.Properties"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"             
             Dialog:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d" 
             ResizeMode="CanResizeWithGrip" 
             d:DesignWidth="755.831" 
             d:DesignHeight="437.372">

    <Controls:MetroWindow.Resources>
        <prop:Resources x:Key="LocalizedStrings"/>
    </Controls:MetroWindow.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding Path=report_settings_title, Source={StaticResource LocalizedStrings}}" FontSize="30" FontWeight="Bold" HorizontalContentAlignment="Center" VerticalContentAlignment="Top"/>

        <Grid Grid.Row="2" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{Binding Path=report_settings_ok, Source={StaticResource LocalizedStrings}}" Width="80" cal:Message.Attach="OnOkClick()"/>
            <Button Grid.Column="1" Content="{Binding Path=report_settings_cancel, Source={StaticResource LocalizedStrings}}" Width="80" Margin="5 0 0 0" cal:Message.Attach="OnCancelClick()"/>

        </Grid>

    </Grid>

</Controls:MetroWindow>

