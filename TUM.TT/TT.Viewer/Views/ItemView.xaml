<ResourceDictionary x:Class="TT.Viewer.Views.ItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org" >
    
    <DataTemplate x:Key="ItemViewTemplate">
        <Grid cal:Bind.ModelWithoutContext="{Binding}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="Score" />
            <TextBlock Grid.Column="1" x:Name="Sets" />
            <TextBlock Grid.Column="2" x:Name="Server" />
            <TextBlock Grid.Column="3" x:Name="Point" />
            <TextBlock Grid.Column="4" x:Name="Length" />

        </Grid>
    </DataTemplate>
</ResourceDictionary>
