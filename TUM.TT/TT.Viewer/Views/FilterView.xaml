<UserControl x:Class="TT.Viewer.Views.FilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Expander x:Name="MenuExpander" ExpandDirection="Right" Margin="0 0 0 0" IsExpanded="True">
        <Expander.Header>
            <TabControl x:Name="HeaderTabControl" TabStripPlacement="Left">
                <TabItem x:Name="ServiceTabHeader" Header="A"/>
                <TabItem x:Name="ReceiveTabHeader" Header="R"/>
                <TabItem x:Name="ThirdTabHeader" Header="3"/>
                <TabItem x:Name="FourthTabHeader" Header="4"/>
                <TabItem x:Name="LastTabHeader" Header="L"/>
                <TabItem x:Name="KombiTabHeader" Header="K"/>
            </TabControl>
        </Expander.Header>
        <TabControl>
            <TabItem Visibility="Collapsed"
                 IsSelected="{Binding ElementName=ServiceTabHeader, Path=IsSelected}">
                <ScrollViewer>
                    <ContentControl x:Name="ServiceView" />
                </ScrollViewer>
            </TabItem>
            <TabItem Visibility="Collapsed"
                 IsSelected="{Binding ElementName=ReceiveTabHeader, Path=IsSelected}">
                Rueckschlag
            </TabItem>
            <TabItem Visibility="Collapsed"
                 IsSelected="{Binding ElementName=ThirdTabHeader, Path=IsSelected}">
                Dritter Ball
            </TabItem>
            <TabItem Visibility="Collapsed"
                 IsSelected="{Binding ElementName=FourthTabHeader, Path=IsSelected}">
                Vierter Ball
            </TabItem>
            <TabItem Visibility="Collapsed"
                 IsSelected="{Binding ElementName=LastTabHeader, Path=IsSelected}">
                Letzter Ball
            </TabItem>
            <TabItem Visibility="Collapsed"
                 IsSelected="{Binding ElementName=KombiTabHeader, Path=IsSelected}">
                Kombi
            </TabItem>
        </TabControl>
    </Expander>
</UserControl>
