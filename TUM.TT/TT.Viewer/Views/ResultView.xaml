<UserControl x:Class="TT.Viewer.Views.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ContentControl x:Name="MiniStatistic" Grid.Row="0" Margin="5 5 5 5" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
        <ContentControl x:Name="ResultTabView" Grid.Row="1" Margin="5 5 5 5" ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
